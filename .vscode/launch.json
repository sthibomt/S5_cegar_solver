{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch S5 Solver",
            "type": "cppdbg",
            "request": "launch",
            "program": "${command:cmake.launchTargetPath}",
            "args": ["P & q"],
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb"        
        }
    ]
}