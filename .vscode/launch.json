{
    "name": "Debug with CMake",
    "type": "cppdbg",
    "request": "launch",
    "program": "${command:cmake.launchTargetPath}",
    "args": ["p -> q"],
    "cwd": "${workspaceFolder}",
    "MIMode": "gdb"
}