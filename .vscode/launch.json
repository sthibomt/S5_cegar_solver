{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug S5 Solver",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/build/s5solver",
            "args": ["p & q"],
            "cwd": "${workspaceFolder}",
            "stopAtEntry": false,
            "externalConsole": false,
            "MIMode": "gdb"
        }
    ]
}